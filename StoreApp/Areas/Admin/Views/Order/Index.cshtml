@model IEnumerable<Order>

@if(Model.Count() > 0)
{
    @foreach(Order order in Model)
    {
        <div class="my-3">
            <partial name="_OrderDetails" model="@order"/>
        </div>
    }
}
else
{
    <div class="alert alert-ınfo">
        <p class="lead">There is no order yet</p>
    </div>
}
